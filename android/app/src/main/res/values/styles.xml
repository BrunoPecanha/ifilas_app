<?xml version="1.0" encoding="utf-8" ?>
<resources>

  <!-- Tema principal da aplicação (mantenha se quiser) -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <!-- Tema usado pela MainActivity -->
  <style
    name="AppTheme.NoActionBar"
    parent="Theme.AppCompat.DayNight.NoActionBar"
  >
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
    <item name="android:background">@null</item>
    
    <!-- As duas linhas mais importantes -->
    <item name="android:statusBarColor">@android:color/white</item>
    <item name="android:windowLightStatusBar">true</item>

    <!-- Opcional: se quiser barra de navegação também clara -->
    <!-- <item name="android:navigationBarColor">@android:color/white</item> -->
    <!-- <item name="android:windowLightNavigationBar">true</item> -->
  </style>

  <!-- Tema de splash (não mexa se está funcionando) -->
  <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
    <item name="android:background">@drawable/splash</item>
  </style>

</resources>
